# Cloudflare Pages 配置
pages_build_output_dir = "dist"

[build]
  command = "npm run build"

# 环境变量
[vars]
  NODE_ENV = "production"
  NPM_VERSION = "10"

# 指定使用 npm 而非 bun
[build.environment]
  NODE_VERSION = "22"
  PREFER_NPM = "true"